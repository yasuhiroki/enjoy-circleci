version: 2.1

executors:
  a:
    docker:
      - image: bash:4.4

commands:
  a:
    steps:
      - run: echo

jobs:
  yasuhiroki:
    executor: a
    steps:
      - run:
          shell: /bin/bash
          command: |
            echo "yasuhiroki"
            echo "- Tw: @duck_yasuhiroki"
            echo
            echo "Yasuhiro Kiyota"
            echo "YASUHIRO KIyota"
            echo "YASUHIRO KI"
            echo "yasuhiroki"
            echo
            echo "cool."

  CircleCI_User_Community:
    executor: a
    steps:
      - run:
          shell: /bin/bash
          command: |
            echo "CircleCI User Communiry の運営メンバー"
            v=2 eval echo 'CircleCI は $((i--)).$((--i)) から使い始めた.'
  Vim:
    executor: a
    steps:
      - run:
          shell: /bin/bash
          command: |
            { Vim | is | my | MOST | favorite | editor; } |& awk '{print $2}' | tr -d : | paste -sd ' '
  ShellScript:
    executor: a
    steps:
      - run:
          shell: /bin/bash
          command: |
            n=1 y='echo CircleCI User Community 第%d回 1>&2\n' s='n=%d y=%s s=%s eval $\47printf "${y}${s}\\n" "${n}" "$((++n))" "${y@Q}" "${s@Q}"\47' eval $'printf "${y}${s}\\n" "${n}" "$((++n))" "${y@Q}" "${s@Q}"' | bash | bash
  Automation:
    executor: a
    steps:
      - run:
          command: |
            : 何もしないことこそ究極の自動化
  Evil:
    executor: a
    steps:
      - run: "false"

workflows:
  introduce:
    jobs:
      - yasuhiroki:
          name: "@yasuhiroki"

      - CircleCI_User_Community:
          name: CircleCI UC
          requires:
            - "@yasuhiroki"

      - Vim:
          requires:
            - CircleCI UC
      - ShellScript:
          requires:
            - CircleCI UC
      - Automation:
          name: 自動化
          requires:
            - CircleCI UC
      - Evil:
          name: 非効率な作業
          requires:
            - CircleCI UC
